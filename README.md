# friscoaddress

This repository now contains two parallel applications:

- `webapp/`, `api/`, and `ui/`: the older Python-based project already in the repo
- `frisco-address-app/`: the new local-first Next.js + Prisma + SQLite + Google Maps application

The new work starts in [frisco-address-app/README.md](/Users/rm/projects/friscoaddress/frisco-address-app/README.md).
